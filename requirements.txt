# ============================================================
# Vivarium Monitoring Pipeline — Requirements
# Python 3.11+
# ============================================================

# --- Core API framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9          # multipart/form-data uploads

# --- Configuration ---
PyYAML>=6.0.1
dotmap>=1.3.30                   # attribute-access dict (config.key syntax)
python-dotenv>=1.0.1             # load .env for local dev

# --- Computer vision ---
opencv-python>=4.9.0
Pillow>=10.3.0
scikit-image>=0.21.0

# --- PyTorch (required for YOLOX) ---
torch>=2.2.0
torchvision>=0.17.0

# --- YOLOX dependencies ---
# NOTE: YOLOX itself must be installed from source (not PyPI):
#   git clone https://github.com/Megvii-BaseDetection/YOLOX.git
#   cd YOLOX && pip install -e . --no-deps
# The packages below are its runtime dependencies:
thop>=0.1.1
ninja>=1.11.0
pycocotools>=2.0.2
tensorboard>=2.12.0
onnx>=1.13.0

# --- ONNX runtime (food/water classifiers) ---
onnxruntime>=1.18.0

# --- Logging ---
loguru>=0.7.2

# --- AWS S3 (optional image source) ---
boto3>=1.34.0

# --- Database ---
sqlalchemy>=2.0.0            # async ORM
aiosqlite>=0.20.0            # async SQLite driver
asyncpg>=0.29.0              # async PostgreSQL driver (swap to Postgres later)

# --- Image hashing (deduplication) ---
imagehash>=4.3.1

# --- Testing ---
pytest>=8.1.0
pytest-asyncio>=0.23.6
pytest-mock>=3.14.0
httpx>=0.27.0                    # async test client for FastAPI

# --- Optional extras ---
# ultralytics>=8.2.0           # YOLOv8/YOLOv8-World (disabled, using YOLOX)
# slowapi>=0.1.9               # rate limiting middleware
